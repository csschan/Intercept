[package]
name = "spending-session"
version = "0.1.0"
description = "Agent Guard — On-chain Spending Session Authority"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "spending_session"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
proc-macro-crate = "=3.2.0"
